const PATHS = {
  HOME: '/',
  PATIENT: {
    SHOW: '/patients/:id',
  }
};

const API_PATHS = {
  PATIENTS: {
    GET: '/api/doctor/patients',
    SHOW: '/api/patients/:id',
  }
}

export {
  PATHS,
  API_PATHS,
};
